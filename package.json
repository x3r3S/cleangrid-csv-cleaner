{
  "name": "cleangrid",
  "version": "1.0.0",
  "private": true,
  "description": "Browser-local CSV cleanup, validation and deduplication workspace.",
  "license": "SEE LICENSE IN PORTFOLIO-REVIEW-LICENSE.md",
  "type": "module",
  "engines": {
    "node": ">=24"
  },
  "scripts": {
    "start": "node scripts/serve.mjs",
    "test": "npm run test:unit && npm run test:browser",
    "test:unit": "node --test tests/domain.test.mjs",
    "test:browser": "playwright test",
    "check": "node --check src/domain.mjs && node --check src/app.mjs && node --check scripts/serve.mjs && node --check scripts/verify-evidence.mjs && node --check playwright.config.mjs && node --check tests/browser/workspace.spec.mjs",
    "verify:evidence": "node scripts/verify-evidence.mjs",
    "verify": "npm run check && npm run test:unit && npm run verify:evidence && npm run test:browser"
  },
  "devDependencies": {
    "@playwright/test": "1.62.1"
  }
}
